<h1>Problems List</h1>
<body>
  <!-- When submit the form, the view rendered will be the index view of our articles resource -->
  <%= form_tag(problems_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Articles" %>
      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
           of using a Rails form helper -->
      <button class="btn" type="submit"><i class="icon-search"></i></button>
    </div>
  <% end %>
</body>
<!--
  <% @problems.each do |problem| %>
  <div class="problem">
    <h1 class="problem-problem_title"><%= link_to problem.problem_title, problem %></h1>
    <p class="problem-problem_description"><%= problem.problem_description %></p>
  </div>
<% end %>
-->
<table>
  <tr>
    <th>Problem</th>
    <th>Area</th>
    <th>Max Score</th>
    <th>Difficulty</th>
  </tr>
 
  <% @problems.each do |problem| %>
    <tr>
      <td><%= link_to problem.problem_title, problem_path(problem.id) %></td>
      <td><%= problem.problem_area %></td>
      <td><%= problem.total_score %></td>
      <td><%= problem.difficulty_level %></td>
    </tr>
  <% end %>
</table>